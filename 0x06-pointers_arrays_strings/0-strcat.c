#include "main.h"
/**
* _memset - fills memory with a constant byte.
* @s: input array
* @b: constant
* @n: n bytes to fill
* Return: s
*/

char *_strcat(char *dest, char *src);
{
	unsigned int i;
	for (i = 0; i < n; i++)
{
	s[i] = b;
}
	return (s);
}

